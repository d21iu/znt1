# 虚拟环境 / 缓存
backend/.venv/
**/__pycache__/
*.pyc

# 密钥与本地环境
backend/.env
.env

# 运行期产物
uploads/
*.tar
*.tar.gz
*.zip

# 文档与样例（不需要进镜像）
docs/
samples/
skills/
*.md
# prompts 是运行时必需（ai_client.py 读 /app/prompts/*.md），必须保留进镜像
!prompts/
!prompts/*.md
